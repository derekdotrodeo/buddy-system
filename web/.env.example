# Cloudflare Tunnel token.
# Create a tunnel in: Cloudflare Zero Trust > Networks > Tunnels, then copy its token.
# In the tunnel's public hostname config, point the route at: http://web:80
# Copy this file to .env and fill in the real value (.env is gitignored).
TUNNEL_TOKEN=replace-with-your-tunnel-token
